let isDone: boolean = false;

// let abb : boolean = new Boolean(1);

let b1 : Boolean  = new Boolean(2)

let b2 : boolean  = Boolean(1)

console.log(b2)


let num1 : number = 6



// string
console.log(name)

let name1 : string = 'cocoa'
let age1 : number = 30

let msg1 : string = 'Hello ${name1} and age is ${age1+1}'


// ts 中的 void 表示没有任何返回值
function alertName() : void {
    alert('My name is Tom')
}

// void 类型只能赋值 undefined 和 null
let nullType : void = undefined

//undefined 类型只能是 undefined  null 类型只能是 null
// 但是与 void 类型不同，undefined  null  是所有类型的子类，
// 也就是说 let a : number = null
let u : undefined = undefined 
let n : null = null


// 但是与 void 类型不同，undefined  null  是所有类型的子类，
// 比如下面的例子

let a : number = null
let b : number = undefined






